<%= form_tag charges_path do %>
  <!-- The form doesn't pass the amount to the create action, so we have to pass it manually. -->
  <%= hidden_field_tag :amount, @stripe_btn_data[:amount] %>

  <h2>Upgrade Your Account</h2>
  <p>Upgrade your account and create private wikis. In addition, add an unlimited number of collaborators to your private wikis.</p>
  <script
    class='stripe-button'
    src="https://checkout.stripe.com/checkout.js"
    data-key="<%= @stripe_btn_data[:key] %>"
    data-image="http://www.trakathletics.com/wp-content/uploads/2014/01/wikipedia_icon_by_emptiedpie-d6lahv4.png"
    data-name="<%= @stripe_btn_data[:name] %>"
    data-amount="<%= @stripe_btn_data[:amount] %>"
    data-description="<%= @stripe_btn_data[:description] %>" >
  </script>
<% end %>